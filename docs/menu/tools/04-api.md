# 도구 — API

## 엔드포인트(예시)

- `GET /mcp/servers` — 등록된 MCP 서버 목록.
- `POST /mcp/servers` — 서버 등록.
- `DELETE /mcp/servers/:id` — 서버 해제.
- `GET /mcp/servers/:id/tools` — 해당 서버 도구 목록.
- `GET /skills` — 스킬 목록.
- `PATCH /skills/:id` — 스킬 설정(활성 등).
